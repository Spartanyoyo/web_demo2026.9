# 视频验收口述提纲（3–5 分钟）

开场先讲清这是什么、规则怎么保证，再演示，最后一句收在取舍上。

## 1. 这是什么（20 秒）

PointDesk 点金台：实习生完成研究 / 建模任务拿积分，用积分兑换数字商品。Next.js + TypeScript + PostgreSQL，积分和兑换全部落库，不接真钱。

## 2. 四条硬规则怎么实现（90 秒）

1. **同一任务不能重复领奖**  
   表 `TaskCompletion` 有 `(userId, taskId)` 唯一约束。领奖在事务里先查再写，撞约束就返回 409，余额不变。

2. **同一兑换请求不能重复扣分或发货**  
   客户端每次新兑换生成 `idempotencyKey`。服务端按 `(userId, idempotencyKey)` 唯一。命中旧单就原样返回，包括原来的提取码。

3. **余额不足**  
   事务内 `SELECT ... FOR UPDATE` 锁用户行，比较余额。不够就写 `REJECTED`，`pointsSpent = 0`，不改余额、不写账本借方。

4. **发放失败**  
   「限量内参」在履约函数里固定失败。写 `FAILED` 并给出原因，同样不扣分。

核心代码在 `src/lib/points.ts`。

## 3. 现场点击顺序

登录 `intern@pointdesk.local` / `intern123`：

1. 任务台领「周一晨会测验」，再点一次看拒绝。
2. 再领「投资备忘录」，余额 50。
3. 兑换 Tombstone，复制凭证。
4. 「用同一请求再提交一次」，凭证不变、余额不变。
5. 兑换 Office Hour，看余额不足。
6. 兑换限量内参，看发放失败且不扣分。
7. 打开账本和兑换结果对账。

## 4. 被问到时怎么答

- **为什么不用 NextAuth？** 只有邮箱密码，JWT Cookie 更少配置，方便讲清楚会话边界。
- **余额会不会并发写丢？** 不会。改余额前锁用户行，加分 / 扣分和写分录在同一事务。
- **失败单为什么也存？** 否则同一幂等键第二次会重新履约。留下原单才能稳定重放。
- **数字商品存在哪？** 不存文件。成功时生成提取码，演示履约，不接对象存储。

## 5. 主动承认的未完成

没有独立对账作业、没有后台上下架、JWT 退出只删 Cookie 不能服务端吊销。对本题范围够用。
