# PointDesk 设计说明

## 产品设定

场景做成「私募实习生工作台」而不是抽象积分商城，是为了让任务、商品和账本语言一致，也方便视频验收时口述。

积分规则保持线性：完成任务入账、兑换商品出账。没有等级、过期或倍率。

| 任务 | 积分 |
| --- | ---: |
| 参加周一晨会测验 | 20 |
| 阅读本周投资备忘录 | 30 |
| 复核 CIM 关键条款 | 60 |
| 完成三表勾稽练习 | 50 |
| 提交行业研究一页纸 | 80 |

| 数字商品 | 售价 | 履约 |
| --- | ---: | --- |
| 模拟项目 Tombstone 徽章 | 25 | 成功发编号 |
| 《并购估值速查手册》 | 40 | 成功发提取码 |
| LBO 模型模板 | 80 | 成功发提取码 |
| 合伙人 Office Hour 预约码 | 120 | 成功发预约码 |
| 限量内参 | 15 | **固定发放失败** |

「限量内参」是故意放进目录的失败路径，用来证明：履约失败时不扣分，并写明原因。

## 数据模型

- `User.balance`：当前可用余额，更新只发生在事务内。
- `TaskCompletion(userId, taskId)` 唯一约束：同一任务奖励不能领两次。
- `Redemption(userId, idempotencyKey)` 唯一约束：同一兑换请求不能处理两次。
- `LedgerEntry`：只在余额真实变动时写入。失败 / 驳回兑换不写借方。

兑换单状态：

- `SUCCESS`：扣分 + 发放凭证
- `REJECTED`：业务拒绝（余额不足等），`pointsSpent = 0`
- `FAILED`：履约失败，`pointsSpent = 0`

## 关键路径

### 领任务

1. 先查是否已领取。
2. `SELECT ... FROM "User" FOR UPDATE` 锁余额行。
3. 写 completion、加余额、写贷方分录。
4. 若并发撞上唯一约束，捕获 `P2002`，返回「已领取」。

### 兑换

1. 先按 `(userId, idempotencyKey)` 查找。命中则原样返回，不再扣分、不再发新码。
2. 事务内再次查找 + 锁用户行。
3. 余额不足 → 写 `REJECTED`，不改余额。
4. 履约失败 → 写 `FAILED`，不改余额。
5. 成功 → 扣余额、写 `SUCCESS`、写借方分录、生成凭证。
6. 并发下唯一约束冲突时，回读已有单据并当作重放。

履约本身是进程内生成提取码，不调用外部支付或对象存储。`kind = flaky` 的商品走失败分支。

## 关键取舍

1. **Prisma + 原生 `FOR UPDATE`，而不是手写全部 SQL。**  
   迁移和类型更省事；需要串行化余额时仍下推到行锁，避免「先读后写」的丢更新。

2. **自建 JWT Cookie，而不是 NextAuth。**  
   本题只有邮箱密码，引入完整认证框架会增加解释成本和配置面。会话只存 `uid/email/name`。

3. **余额冗余在用户表，账本做流水。**  
   读余额便宜；正确性靠「改余额和写分录必须在同一事务」。没有做账本重放对账作业。

4. **失败兑换也落库。**  
   否则同一幂等键第二次进来会重新走履约。验收要求「重复提交不重复扣分 / 发货」，保留原单是最直接的实现。

5. **不做库存、过期、部分成交。**  
   数字商品视为无限供给。限量内参只模拟通道失败，不模拟卖光。

6. **本地运行优先于部署。**  
   提供 `docker-compose` 只为 Postgres。应用本身 `next dev` 即可。

## 安全与资金边界

- 密码 bcrypt 哈希。
- Cookie `httpOnly` + `sameSite=lax`。
- 所有写接口校验会话。
- 无支付通道、无真实资金、无第三方履约。

## 未解决问题

- 没有独立对账任务去重建 `User.balance` 并与分录核对。
- 没有后台管理端上下架任务 / 商品。
- JWT 无法主动吊销（退出只删 Cookie）。
- 未做验证码、邮箱验证、密码重置。
- 未做生产级观测（结构化日志、tracing）。
- GitHub Public Repo 需要用你自己的账号推送；本目录已可直接 `git init`。

## 核心实现位置

- 领域规则：`src/lib/points.ts`
- 数据模型：`prisma/schema.prisma`
- 会话：`src/lib/session.ts`
- 兑换 / 任务 API：`src/app/api/**`
